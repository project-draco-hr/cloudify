{
  final GridServiceContainerOptions options=new GridServiceContainerOptions().vmInputArgument("-Xmx" + CloudifyConstants.DEFAULT_LOCALCLOUD_REST_WEBUI_SPACE_MEMORY_IN_MB + "m").vmInputArgument("-Dcom.gs.zones=rest,cloudifyManagementSpace,webui");
  agent.startGridServiceAndWait(options);
}
