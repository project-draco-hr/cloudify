{
  if (isLocalCloud) {
    return !ServiceUtils.isPortFree(OpenspacesConstants.DEFAULT_LOCALCLOUD_LUS_PORT);
  }
  return !ServiceUtils.isPortFree(this.cloud.getConfiguration().getComponents().getDiscovery().getDiscoveryPort());
}
