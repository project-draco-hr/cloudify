{
  CustomNode selectedNode=null;
  for (  final CustomNode node : getAllNodes()) {
    if (node.getId().equalsIgnoreCase(id)) {
      selectedNode=node;
      break;
    }
  }
  return selectedNode;
}
