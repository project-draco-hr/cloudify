{
  CustomNode selectedNode=null;
  for (  final CustomNode node : getAllNodesByTemplateName(templateName)) {
    if (StringUtils.isNotBlank(node.getResolvedIP()) && node.getResolvedIP().equalsIgnoreCase(ipAddress)) {
      selectedNode=node;
      break;
    }
  }
  return selectedNode;
}
