{
  try {
    this.storageProvisioning.deleteVolume(volumeDetails.getId(),1,TimeUnit.MINUTES);
  }
 catch (  Exception e) {
    logger.log(Level.WARNING,"Volume attachment failed. " + "Failed cleaning volume after attachment failure. ( " + volumeDetails.getId() + "). Error was: "+ e.getMessage(),e);
  }
}
