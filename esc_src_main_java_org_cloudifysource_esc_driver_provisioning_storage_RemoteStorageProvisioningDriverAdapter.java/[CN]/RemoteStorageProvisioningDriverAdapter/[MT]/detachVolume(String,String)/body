{
  try {
    storageProvisioningDriver.detachVolume(volumeId,ip,DEFAULT_STORAGE_OPERATION_TIMEOUT,TimeUnit.MILLISECONDS);
  }
 catch (  final Exception e) {
    throw new RemoteStorageOperationException("Failed detaching volume with id " + volumeId + " to instance with ip "+ ip,e);
  }
}
