{
  Admin admin=validationContext.getAdmin();
  final ElasticServiceManager esm=getESM(admin);
  if (esm == null) {
    throw new IllegalStateException("Could not find an ESM in the cluster. Groups: " + Arrays.toString(admin.getGroups()));
  }
}
