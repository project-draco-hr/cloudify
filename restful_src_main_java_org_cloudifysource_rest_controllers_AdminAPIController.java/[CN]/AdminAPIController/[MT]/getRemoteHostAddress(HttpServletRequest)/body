{
  final String host=httpServletRequest.getServerName();
  final int port=httpServletRequest.getServerPort();
  String url=IPUtils.getRestProtocol() + "://" + IPUtils.getSafeIpAddress(host)+ ":"+ port;
  return url;
}
