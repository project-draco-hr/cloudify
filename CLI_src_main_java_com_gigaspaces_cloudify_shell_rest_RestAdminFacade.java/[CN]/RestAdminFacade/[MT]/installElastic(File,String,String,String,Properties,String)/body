{
  String url=SERVICE_CONTROLLER_URL + "applications/" + applicationName+ "/services/"+ serviceName;
  client.postFile(url + "?zone=" + zone+ "&template="+ templateName,packedFile,contextProperties);
}
