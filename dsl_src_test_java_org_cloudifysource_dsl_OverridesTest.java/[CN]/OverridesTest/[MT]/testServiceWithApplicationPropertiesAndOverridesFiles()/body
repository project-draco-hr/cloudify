{
  try {
    testDSLOverrides(APPLICATION_SERVICE_PATH,null,Service.class,APPLICATION_SERVICE_OVERRIDEN_PROEPRTIES_MATCHING_FIELDS);
  }
 catch (  final DSLException e) {
    fail("Failed to read application " + APPLICATION_SERVICE_PATH + e);
  }
}
