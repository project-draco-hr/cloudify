{
  if (StringUtils.isBlank(name)) {
    throw new DSLValidationException("Application.validateName: The application's name " + (name == null ? "is missing" : "is empty"));
  }
  DSLUtils.validateRecipeName(name);
}
