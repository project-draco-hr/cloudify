{
  try {
    if (admin != null) {
      admin.close();
    }
  }
 catch (  Exception ex) {
    logger.info("ByonProvisioningDriver.close() failed to close agent");
  }
  if (deployer != null) {
    deployer.close();
  }
}
