{
  String volumeId=remoteStorageProvisioningDriver.createVolume(templateName,serviceContext.getLocationId(),timeoutInMillis);
  setVolumeId(volumeId);
  changeVolumeState(VolumeState.CREATED);
  return volumeId;
}
