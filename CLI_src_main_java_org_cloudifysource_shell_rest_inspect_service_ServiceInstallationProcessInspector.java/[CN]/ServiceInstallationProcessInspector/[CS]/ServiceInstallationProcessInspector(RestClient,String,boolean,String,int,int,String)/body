{
  super(restClient,deploymentId,(applicationName != null ? applicationName : CloudifyConstants.DEFAULT_APPLICATION_NAME),verbose,createOneEntryMap(serviceName,plannedNumberOfInstances),createOneEntryMap(serviceName,currentNumberOfInstances));
  this.serviceName=serviceName;
}
