{
  try {
    getAzureDeployment().createDeployment(azureHostedServiceName,azureDeploymentName,azureDeploymentName,AzureSlot.fromString(azureDeploymentSlotName),cscfgFile,cspkgUri);
  }
 catch (  AzureDeploymentException e) {
    throw new CLIException(e);
  }
}
