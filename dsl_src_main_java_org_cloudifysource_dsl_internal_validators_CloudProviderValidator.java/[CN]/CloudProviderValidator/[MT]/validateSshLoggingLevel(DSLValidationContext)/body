{
  if (!entity.getSshLoggingLevel().matches("INFO|FINE|FINER|FINEST|WARNING|DEBUG")) {
    throw new DSLValidationException("sshLoggingLevel \"" + entity.getSshLoggingLevel() + "\" is invalid, "+ "supported values are: INFO, FINE, FINER, FINEST, WARNING, DEBUG");
  }
}
