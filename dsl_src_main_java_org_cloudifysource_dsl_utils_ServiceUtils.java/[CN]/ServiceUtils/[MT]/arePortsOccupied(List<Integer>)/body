{
  int portCounter=0;
  for (  final int port : portList) {
    if (isPortOccupied(port)) {
      portCounter++;
    }
    if (portCounter == portList.size()) {
      return true;
    }
  }
  return false;
}
