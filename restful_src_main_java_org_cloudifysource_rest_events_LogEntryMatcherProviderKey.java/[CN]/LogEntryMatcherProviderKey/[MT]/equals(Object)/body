{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  LogEntryMatcherProviderKey that=(LogEntryMatcherProviderKey)o;
  return container.getUid().equals(that.container.getUid()) && deploymentId.equals(that.deploymentId);
}
