{
  try {
    final MemcachedClient memClient=new XMemcachedClient("localhost",11211);
    MemcacheTest.testMemcachedPerformance(memClient);
    memClient.shutdown();
  }
 catch (  final Exception ex) {
    ex.printStackTrace();
    System.exit(1);
  }
  System.exit(0);
}
