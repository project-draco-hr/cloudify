{
  if (deploymentId == null) {
    logger.warning("[" + methodName + "] - deployment ID is missing.");
    RestClientException restClientException=MessagesUtils.createRestClientException(CloudifyErrorMessages.MISSING_DEPLOYMENT_ID.getName(),methodName);
    throw restClientException;
  }
}
