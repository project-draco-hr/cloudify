{
  String url=getFormattedUrl(versionedDeploymentControllerUrl,GET_SERVICE_DESCRIPTION_URL_FORMAT,appName,serviceName);
  return executor.get(url,new TypeReference<Response<ServiceDescription>>(){
  }
);
}
