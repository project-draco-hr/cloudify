{
  String url=getFormattedUrl(versionedDeploymentControllerUrl,GET_DEPLOYMENT_EVENTS_URL_FORMAT,deploymentId,String.valueOf(from),String.valueOf(to));
  return executor.get(url,new TypeReference<Response<DeploymentEvents>>(){
  }
);
}
