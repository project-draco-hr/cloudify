{
  final String removeTempalteUrl=getFormattedUrl(versionedTemplatesControllerUrl,REMOVE_TEMPALTE_INTERNAL_URL_FORMAT);
  executor.delete(removeTempalteUrl,new TypeReference<Response<Void>>(){
  }
);
}
