{
  if (this.serviceProcessingUnit == null) {
    this.serviceProcessingUnit=memcached;
  }
 else   if (memcached != null) {
    throw new IllegalStateException("DSL File contains more then one ProcessingUnit state");
  }
}
