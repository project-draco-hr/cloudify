{
  final DSLReader dslReader=getDslReader(dslFile,workDir,admin,clusterInfo,propertiesFileName,isRunningInGSC,DSLReader.SERVICE_DSL_FILE_NAME_SUFFIX,null,null);
  final Service service=dslReader.readDslEntity(Service.class);
  return new DSLServiceCompilationResult(service,dslReader.getContext(),dslFile);
}
