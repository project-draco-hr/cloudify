{
  String iconUrlStr=getContextPropertyValue(processingUnit,CloudifyConstants.SERVICE_EXTERNAL_FOLDER + CloudifyConstants.CONTEXT_PROPERTY_SERVICE_ICON);
  if (iconUrlStr == null) {
    return "";
  }
  return iconUrlStr;
}
