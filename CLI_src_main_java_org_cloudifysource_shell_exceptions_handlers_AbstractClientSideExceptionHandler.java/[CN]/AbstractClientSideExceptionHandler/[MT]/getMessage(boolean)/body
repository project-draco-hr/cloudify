{
  if (verbose) {
    final String stackTrace=getVerbose();
    if (stackTrace != null) {
      return getFormattedMessage() + " : " + stackTrace;
    }
    return getFormattedMessage();
  }
  return getFormattedMessage();
}
