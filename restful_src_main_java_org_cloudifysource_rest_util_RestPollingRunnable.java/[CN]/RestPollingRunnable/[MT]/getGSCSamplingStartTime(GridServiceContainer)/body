{
  if (this.gscStartTimeMap.containsKey(gsc.getUid())) {
    return this.gscStartTimeMap.get(gsc.getUid());
  }
 else {
    Date date=new Date(new Date().getTime() + gsc.getOperatingSystem().getTimeDelta());
    this.gscStartTimeMap.put(gsc.getUid(),date);
    return date;
  }
}
