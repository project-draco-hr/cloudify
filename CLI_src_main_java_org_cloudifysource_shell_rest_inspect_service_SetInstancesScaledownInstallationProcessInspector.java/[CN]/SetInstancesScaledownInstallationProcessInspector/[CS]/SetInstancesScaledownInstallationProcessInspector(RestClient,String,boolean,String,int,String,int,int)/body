{
  super(restClient,deploymentId,verbose,serviceName,plannedNumberOfInstances,currentNumberOfInstances,applicationName);
  setLastEventIndex(currentEventIndex);
  this.plannedNumberOfInstances=plannedNumberOfInstances;
}
