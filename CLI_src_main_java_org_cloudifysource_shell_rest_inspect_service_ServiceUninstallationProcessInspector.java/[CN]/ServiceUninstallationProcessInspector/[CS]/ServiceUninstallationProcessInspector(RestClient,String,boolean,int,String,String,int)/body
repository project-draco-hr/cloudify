{
  super(restClient,deploymentId,applicationName,verbose,createOneEntryMap(serviceName,0),createOneEntryMap(serviceName,currentNumberOfRunningInstance));
  this.serviceName=serviceName;
  setLastEventIndex(nextEventIndex);
}
