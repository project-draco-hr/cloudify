{
  try {
    return getAzureDeployment().getDeploymentStatus(azureHostedServiceName,AzureSlot.fromString(azureDeploymentSlotName));
  }
 catch (  AzureDeploymentException e) {
    throw new CLIException(e);
  }
}
