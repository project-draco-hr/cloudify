{
  logger.warning("Accessing the admin object underlying a timed admin is not recommended!" + " This action is bypasses the timing mechanism and might result in a premature admin termination!");
  initAdmin();
  return admin;
}
