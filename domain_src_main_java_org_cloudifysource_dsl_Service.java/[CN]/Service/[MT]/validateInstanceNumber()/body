{
  if (this.numInstances > this.maxAllowedInstances) {
    throw new DSLValidationException("The requested number of instances (" + this.numInstances + ") exceeds the maximum number of instances allowed"+ " ("+ this.maxAllowedInstances+ ") for service "+ this.name+ ".");
  }
}
