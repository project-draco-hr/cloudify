{
  if (StringUtils.isBlank(name)) {
    throw new DSLValidationException("Service.validateName: The service's name " + (name == null ? "is missing" : "is empty"));
  }
  DSLUtils.validateRecipeName(name);
}
