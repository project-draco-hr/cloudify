{
  try {
    @SuppressWarnings("unchecked") List<String> applications=(List<String>)client.get("/service/applications");
    return applications;
  }
 catch (  final ErrorStatusException e) {
    throw new CLIStatusException(e,e.getReasonCode(),e.getArgs());
  }
}
