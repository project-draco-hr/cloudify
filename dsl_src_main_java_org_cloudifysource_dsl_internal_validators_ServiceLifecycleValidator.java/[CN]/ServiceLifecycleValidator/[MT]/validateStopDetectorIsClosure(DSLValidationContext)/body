{
  if ((this.entity.getStopDetection() != null) && (!(this.entity.getStopDetection() instanceof ClosureExecutableEntry))) {
    throw new DSLValidationException("The stop detection field only supports execution of closures");
  }
}
