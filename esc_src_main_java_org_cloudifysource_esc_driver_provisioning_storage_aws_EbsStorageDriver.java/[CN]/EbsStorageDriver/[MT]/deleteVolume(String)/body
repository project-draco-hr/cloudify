{
  try {
    logger.fine("deleting volume with id " + volumeId);
    this.ebsClient.deleteVolumeInRegion(null,volumeId);
  }
 catch (  Exception e) {
    logger.log(Level.WARNING,"Failed deleting volume with ID " + volumeId + " Reason: "+ e.getMessage(),e);
    throw new StorageProvisioningException("Failed deleting volume with ID " + volumeId + " Reason: "+ e.getMessage(),e);
  }
}
