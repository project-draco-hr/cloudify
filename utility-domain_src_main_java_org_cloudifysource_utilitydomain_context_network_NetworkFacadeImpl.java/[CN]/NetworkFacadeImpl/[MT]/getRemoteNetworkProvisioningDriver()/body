{
  if (this.remoteNetworkProvisioningDriver == null) {
    throw new IllegalStateException("No network provisioning driver configured. " + "remote network provisioning calls are not possible");
  }
  return this.remoteNetworkProvisioningDriver;
}
