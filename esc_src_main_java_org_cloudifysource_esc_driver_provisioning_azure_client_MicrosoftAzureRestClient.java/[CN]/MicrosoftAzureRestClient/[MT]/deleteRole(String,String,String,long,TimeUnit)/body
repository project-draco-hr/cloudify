{
  ClientResponse response=doDelete(subscriptionId + "/services/hostedservices/" + cloudServiceName+ "/deployments/"+ deploymentName+ "/roles/"+ roleName);
  String requestId=extractRequestId(response);
  waitForRequestToFinish(requestId,timeout,timeunit);
}
